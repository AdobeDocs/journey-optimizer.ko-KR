---
title: 오퍼 카탈로그 내보내기 시작
description: 이 섹션에는 결정을 위해 내보낸 데이터 세트에 사용된 모든 필드가 나열됩니다.
translation-type: tm+mt
source-git-commit: 70c172e19d5900c898d4850801468a2e186e682d
workflow-type: tm+mt
source-wordcount: '1483'
ht-degree: 3%

---

# 데이터 집합 {#decisions-dataset} 결정

오퍼가 수정될 때마다 결정을 위해 자동으로 생성된 데이터 세트가 업데이트됩니다.

![](../../assets/dataset-activities.png)

데이터 세트에서 가장 최근에 성공한 일괄 처리가 오른쪽에 표시됩니다. 데이터 세트에 대한 스키마의 계층 구조 보기가 왼쪽 창에 표시됩니다.

>[!NOTE]
>
>[이 섹션](../export-catalog/access-dataset.md)에서 오퍼 라이브러리의 각 개체에 대해 내보낸 데이터 집합에 액세스하는 방법을 알아봅니다.

다음은 **[!UICONTROL Decision Object Repository - Decisions]** 데이터 집합(이전의 결정 개체 저장소 - 활동이라고 함)에 사용할 수 있는 모든 필드의 목록입니다.

<!--A decision (formerly known as offer decision) is used to control the decisioning process. It specifies the filter applied to the total inventory to narrow down offers by topic/category, the placement to narrow down the inventory to those offers that technically fit into the reserved space for the offer and specifies a fallback option should the combined constraints disqualify all available personalization offers.-->

## 식별자

**필드:** _id 
**제목:** 식별자 
**설명:** 레코드에 대한 고유한 식별자입니다.
**유형:** 문자열

## _경험

**필드:** _경험 
**유형:** 객체

### 의사 결정

**필드:** 의사 결정 
**유형:** 객체

#### 기준

**필드:** 기준 
**** 제목:
**기준** 설명:각 항목에 제한 조건 세트가 포함된 결정 기준 세트를 정의합니다.
**유형:** 배열

* **description**

   **필드:** 설명
   **제목:** 설명
   **설명:** 기준 설명입니다. 이 기준은 어떻게, 왜 이 기준이 만들어졌는지, 그리고 그것이 결정에 어떤 영향을 주는지에 대한 사람이 읽을 수 있는 의도를 전달하는 데 사용됩니다.
   **유형:** 문자열

* **optionSelection**

   **필드:** optionSelection
   **제목:** 옵션 선택
   **설명:** 이 선택 사항은 이 컨텍스트에서 옵션의 유효성/적용 가능성을 정의합니다.
   **유형:** 객체

   * **설명**

      **필드:** 설명
      **제목:** 설명
      **설명:** 옵션 선택 설명입니다. 이 옵션은 이 옵션 선택이 어떻게 구성되었는지 및/또는 어떤 옵션이 일치하는지 사람이 읽을 수 있는 의도를 전달하는 데 사용됩니다.
      **유형:** 문자열

   * **옵션 필터**

      **필드:** 필터
      **제목:** 옵션 필터
      **설명:** 첨부된 태그를 사용하여 인벤토리의 옵션과 일치하는 태그 기반 필터에 대한 참조입니다. 값은 참조되는 결정 규칙의 URI(@id)입니다. 스키마 https://ns.adobe.com/experience/decisioning/filter을 참조하십시오.
      **유형:** 문자열

   * **프로파일 제한 유형**

      **필드:** optionSelectionType
      **제목:** 프로필 제한 유형
      **설명:** 현재 설정된 제한 여부와 제약 조건을 표현하는 방법을 결정합니다. 필터 쿼리 또는 하나 이상의 세그먼트 멤버십을 통해 할 수 있습니다.
      **유형:** 문자열
      **가능한 값:** &quot;directList&quot;, &quot;filter&quot;
      **기본값:** &quot;없음&quot;

   * **옵션 목록**

      **필드:** 옵션
      **제목:** 옵션 목록
      **설명:** 필터 쿼리를 평가하지 않고 옵션을 직접 지정하는 목록입니다. 옵션 목록 또는 옵션 필터 규칙을 지정할 수 있습니다.
      **유형:** 배열

      <!--Missing title under Option List? Desc = An identifier of an decision option entity. The value value refers to an `@id` property of a decision option. Type: string-->

* **배치**

   **필드:** 배치
   **제목:** 배치 제한
   **설명:** 배치 제약 조건은 이 기준이 나열된 배치에만 적용됨을 나타냅니다. 대상 배치가 `xdm:placements` 목록에 있는 경우에만 고려되는 옵션 선택 사항입니다. 그렇지 않으면 전체 결정 기준을 건너뜁니다. &#39;xdm:placement&#39; 목록이 생략되거나 비어 있으면 대상 배치에 대한 기준이 고려됩니다. 여기에 나열된 배치는 옵션 선택에 대한 암시적 기준을 적용합니다. 고려되는 옵션에는 대상 배치를 위한 표현이 있어야 합니다.
   **유형:** 배열

   * **배치 식별자**

   **제목:** 배치 식별자
   **설명:** 배치 엔티티에 대한 참조입니다. 값은 참조되는 위치의 URI(@id)입니다. 스키마 https://ns.adobe.com/experience/decisioning/placement을 참조하십시오.
   **유형:** 문자열

* **profileConstraints**

   **필드:** profileConstraints
   **제목:** 프로필 제한
   **설명:** 프로필 제약 조건은 현재 이 컨텍스트에서 옵션 선택이 이 프로필 ID에 적합한지를 결정합니다. 프로파일 제약 조건이 각 옵션의 값을 고려할 필요가 없는 경우(예: 옵션 선택에서 옵션이 불변인 경우, &#39;false&#39;로 평가되는 프로파일 제약 조건은 전체 옵션 선택을 취소합니다.) 반면에 매개 변수로 옵션을 선택하는 프로파일 제한 규칙은 옵션 선택의 각 자격 옵션에 대해 평가됩니다.
   **유형:** 객체

   * **설명**

      **필드:** 설명
      **제목:** 설명
      **설명:** 프로필 제한 설명입니다. 이 프로필 제약 조건이 어떻게 구성되었는지 및/또는 어떤 옵션이 포함되거나 제외될 것인지에 대해 읽을 수 있는 의도를 전달하는 데 사용됩니다.
      **유형:** 문자열

   * **자격 조건 규칙**

      **필드:** 자격 조건
      **제목:** 자격 조건 규칙
      **설명:** 주어진 프로필 및/또는 기타 상황에 맞는 XDM 개체에 대해 true 또는 false로 평가되는 결정 규칙에 대한 참조입니다. 이 규칙은 해당 옵션이 지정된 프로필에 적합한지를 결정하는 데 사용됩니다. 값은 참조되는 결정 규칙의 URI(@id)입니다. 스키마 https://ns.adobe.com/experience/decisioning/rule을 참조하십시오.
      **유형:** 문자열

   * **프로파일 제한 유형**

      **필드:** profileConstraintType
      **제목:** 프로필 제한 유형
      **설명:** 현재 설정된 제한 여부와 제약 조건을 표현하는 방법을 결정합니다. 규칙 또는 하나 이상의 세그먼트 멤버십을 통해 가능합니다.
      **유형:** 문자열
      **가능한 값:**
      * &quot;없음&quot;
      * &quot;eligibilityRule&quot;:&quot;프로필 제약 조건은 제한 작업이 허용되기 전에 true로 평가해야 하는 단일 규칙으로 표현됩니다.&quot;
      * &quot;anySegments&quot;:&quot;프로파일 제약 조건은 하나 이상의 세그먼트로 표현되며 제한 작업이 허용되기 전에 프로파일이 하나 이상의 세그먼트의 구성원이어야 합니다.&quot;
      * &quot;allSegments&quot;:&quot;프로필 제약 조건은 하나 이상의 세그먼트로 표현되며 제한 작업이 허용되기 전에 프로파일이 모든 세그먼트의 구성원이어야 합니다.&quot;
      * &quot;규칙&quot;:&quot;프로필 제약 조건은 여러 다른 규칙(예: 자격, 적용 가능성, 적합성)으로 표현되므로 제한 조치를 허용하기 전에 모두 true로 평가해야 합니다.&quot;
         **기본값:** &quot;없음&quot;
   * **세그먼트 식별자**

      **필드:** segmentId
      **제목:** 세그먼트 식별자
      **설명:** 세그먼트의 식별자입니다.
      **유형:** 배열

      * **식별자**

         **필드:** _id
         **제목:** 식별자
         **설명:** 관련 네임스페이스에 있는 세그먼트의 ID입니다.
         **유형:** 문자열

      * **네임스페이스**

         **필드:** 네임스페이스
         **제목:** 네임스페이스
         **설명:** 속성과 연결된  `xid` 네임스페이스입니다.
         **유형:** 객체
         **필수:** &quot;code&quot;

         * **코드**

            **필드:** 코드
            **제목:** 코드
            **설명:** 코드는 네임스페이스에 대해 읽을 수 있는 식별자이며 ID 그래프 처리에 사용되는 기술 네임스페이스 id를 요청하는 데 사용할 수 있습니다.
            **유형:** 문자열
      * **경험 식별자**

         **필드:** xid
         **제목:** 경험 식별자
         **설명:** 이 값은 모든 네임스페이스의 모든 네임스페이스 범위 식별자에 대해 고유한 상호 네임스페이스 식별자를 나타냅니다.
         **유형:** 문자열



* **순위**

   **필드:** 등급
   **제목:** 등급 세부 사항
   **설명:** 등급(우선순위). 의사 결정 기준의 컨텍스트를 고려하여 \&quot;best option\&quot;을 선택하는 방법을 정의합니다. 프로필 제약 조건을 충족하는 선택한 모든 옵션 중에서 순위를 선택하여 제안 대상 상위(또는 상위 N) 옵션을 결정합니다.
   **유형:** 객체

   * **주문**

      **필드:** 순서
      **제목:** 주문 평가
      **설명:** 하나 이상의 결정 옵션에 대한 상대적 순서를 평가합니다. 서수 값이 낮은 옵션에는 더 높은 옵션이 선택됩니다. 이 방법으로 결정된 값은 순서를 지정할 수 있지만 그 사이의 거리는 측정할 수 없으며 합이나 제품을 계산할 수 없습니다. 중간값과 모드는 서수 데이터에 사용할 수 있는 중앙 경향의 유일한 측정값입니다.
      **유형:** 객체

      * **점수 지정 함수**

         **필드:** 함수
         **제목:** 채점 함수
         **설명:** 이 결정 옵션에 대한 숫자 점수를 계산하는 함수에 대한 참조입니다. 그러면 결정 옵션이 해당 점수에 따라 순서(등급)가 지정됩니다. 이 속성의 값은 옵션 시 호출할 함수의 URI(@id)입니다. 스키마 https://ns.adobe.com/experience/decisioning/function을 참조하십시오.
         **유형:** 문자열

      * **주문 평가 유형**

         **필드:** orderEvaluationType
         **제목:** 주문 평가 유형
         **설명:** 사용할 주문 평가 메커니즘, 결정 옵션의 정적 우선순위, 모든 옵션에 대한 숫자 값을 계산하는 점수 함수 또는 순서를 지정할 목록을 받는 등급 전략을 지정합니다.
         **유형:** 문자열
         **가능한 값:** &quot;static&quot;, &quot;scoringFunction&quot;, &quot;rankingStrategy&quot;

      * **순위 전략**

         **필드:** 등급 전략
         **제목:** 등급 전략
         **설명:** 결정 옵션 목록의 등급을 매기는 전략에 대한 참조입니다. 결정 옵션이 순서대로 표시됩니다. 이 속성의 값은 옵션 시 호출할 함수의 URI(@id)입니다. 스키마 https://ns.adobe.com/experience/decisioning/rankingStrategy을 참조하십시오.
         **유형:** 문자열
   * **우선순위**

      **필드:** 우선 순위
      **제목:** 우선 순위
      **설명:** 다른 모든 옵션에 상대적인 단일 결정 옵션의 우선 순위입니다. 순서 함수를 지정하지 않은 옵션은 이 속성을 사용하여 우선 순위가 지정됩니다. 우선 순위가 높은 옵션은 낮은 우선 순위 옵션 전에 선택됩니다. 두 개 이상의 적격 옵션이 가장 높은 우선 순위 값을 공유하는 경우 하나는 균일한 임의 상태로 선택되고 결정 제안에 사용됩니다.
      **유형:** 정수
      **최소 값:** 0
      **기본값:** 0


#### 활동 종료 날짜 및 시간

**필드:** 종료 
**시간** 제목:
**활동 종료 날짜 및 시간** 설명:결정 종료 날짜 및 종료 시간입니다. 이 속성에는 http://schema.org/Action에 정의된 schema.org의 &#39;endTime&#39; 속성의 의미 체계가 있습니다.
**유형:** 문자열

#### 대체 옵션

**필드:** 폴백 
**제목:** 폴백 옵션 
**설명:** 이 결정 컨텍스트에서 결정을 내릴 때 사용되는 폴백 옵션에 대한 참조로는 일반 옵션 중 어떤 것도 적격하지 않습니다(일반적으로 하드 제약조건이 적용된 경우 이러한 상황이 발생합니다). 값은 참조되는 폴백 옵션의 URI(@id)입니다.
**유형:** 문자열

#### 활동 이름

**필드:** 이름 
**제목:** 활동 이름 
**설명:** 다양한 사용자 인터페이스에 표시되는 의사 결정 이름입니다.
**유형:** 문자열

#### 활동 시작 날짜 및 시간

**필드:** startTime 
**제목:** 활동 시작 날짜 및 시간 
**설명:** 결정 시작 날짜 및 종료 시간입니다. 이 속성에는 http://schema.org/Action에 정의된 schema.org의 &#39;startTime&#39; 속성의 의미 체계가 있습니다.
**유형:** 문자열

## _repo

**필드:** _repo 
**유형:** 객체

### 활동 태그

**필드:** 태그 
**제목:** 활동 ETag 
**** 설명:스냅샷을 가져올 때 결정 개체가 있었던 개정입니다.
**유형:** 문자열
